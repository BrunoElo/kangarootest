import { AcceptedRangeDirective } from './accepted-range.directive';

describe('AcceptedRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new AcceptedRangeDirective();
    expect(directive).toBeTruthy();
  });
});
